{
  "version": 2,
  "builds": [
    {
      "src": "Simap_UI/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "Simap_UI/app.py"
    }
  ],
  "env": {
    "FLASK_ENV": "production"
  }
}