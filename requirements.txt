# SIMAP Project - Dependencies
# =============================
# Web-App-Runtime kommt aus simap_ui/requirements.txt;
# dieses File ergänzt Embedding-Pipeline und Notebook-Extras.

-r simap_ui/requirements.txt

# ----------------------------------------------------------------------------
# Embeddings / public.embeddings rebuild pipeline (BAAI/bge-m3, 1024d)
#   Run:  python -m embeddings.build_embeddings --source all
# ----------------------------------------------------------------------------
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.3.0
huggingface_hub>=0.24.0
requests>=2.31.0
tqdm>=4.66.0

# ----------------------------------------------------------------------------
# Notebooks / Analyse
# ----------------------------------------------------------------------------
jupyter>=1.1.0
pandas>=2.2.0
matplotlib>=3.10.0
seaborn>=0.13.0
